# Terraform
infra/terraform.tfvars
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.backup
infra/.terraform.lock.hcl

# Env files
**/.env

# SOPS — plaintext secrets nunca commitados, só o .sops.yaml (criptografado)
secrets/apps.yaml

# OS
.DS_Store
