---
# Valores públicos
grafana_admin_user: admin
postgres_user: homelab
postgres_db: homelab

# grafana_admin_password e postgres_password vêm do CI:
#   sops --decrypt secrets/apps.sops.yaml > /tmp/secrets.yml
#   ansible-playbook deploy.yml --extra-vars @/tmp/secrets.yml
